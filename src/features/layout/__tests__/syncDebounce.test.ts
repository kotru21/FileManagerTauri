import { describe, expect, it } from "vitest"

// Duplicate test removed — keep a skipped suite to avoid vitest "no test suite found" error
describe.skip("syncDebounce duplicate (removed)", () => {
  it("skipped", () => {
    expect(true).toBe(true)
  })
})
